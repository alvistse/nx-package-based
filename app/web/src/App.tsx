import { Counter } from "ui/Counter"

function App() {
    return (
        <main className="h-screen bg-zinc-100 flex justify-center items-center">
            <Counter />
        </main>
    )
}

export default App
